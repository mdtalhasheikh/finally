# FinAlly Project - the Finance Ally

All project documentation is in the `planning` directory.

The key document is `planning/PLAN.md`; the market data component has been completed and is summarized in `planning/MARKET_DATA_SUMMARY.md` with more details in `planning/archive/`. Consult these docs only when required. The remainder of the platform is still to be developed.